# TIC Tehran backbone DNS forwarder
# Forwards all queries to Docker's embedded DNS (127.0.0.11)
# which has access to the real internet via the Docker bridge.

no-resolv
server=127.0.0.11

log-queries
log-facility=/var/log/dnsmasq.log
