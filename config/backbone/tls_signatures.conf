# TLS Handshake Signatures (JA3/JA4-like mock patterns)
# Format: label:hex_pattern
# Matches specific patterns in TLS ClientHello to identify "unauthorized" clients.
# Note: hex patterns must NOT contain spaces (to avoid shell word-splitting).

# Mock: Go-based TLS client (often used in tools)
go_tls_sim:|14000000000000000000000000000000000000000000|
# Mock: Python-requests handshake (simulated)
python_tls_sim:|001d00170018|
# Mock: Known old VPN client fingerprint (simulated)
vpn_handshake_sim:|000500050100000000|
