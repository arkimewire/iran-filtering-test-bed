# Encapsulated Protocol Signatures (Hex strings for iptables matching)
# Format: label:hex_pattern
# Note: hex patterns must NOT contain spaces (to avoid shell word-splitting).
# Below are some *simulated* signatures for common non-obfuscated proxy handshakes.

# VMess non-TLS handshake (simplified/simulated)
vmess_sim:|01000000000000000000000000000001|
# Shadowsocks-like salt-based packet start (simulated)
ss_sim:|53530000|
# VLESS-like header (simulated)
vless_sim:|0001000000000000|
