# Backbone DNS forwarder configuration
# Forwards all queries to Docker's embedded DNS (127.0.0.11)
# which has access to the real internet.

no-resolv
server=127.0.0.11

log-queries
log-facility=/var/log/dnsmasq.log
