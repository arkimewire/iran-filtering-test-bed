# List of IPs suspected of being VPN/Proxy servers based on active probing.
# Any IP on this list will be dropped at the backbone.
#
# Format: IP/CIDR [comment]

203.0.113.100  # Probed: VMess server confirmed
203.0.113.101  # Probed: Shadowsocks server confirmed
