# Behavioral Pattern Recognition Configuration
# Simulates statistical filtering with randomized drops and rate limiting.

# Percentage of large packets to randomly drop (0-100)
drop_percent=5
# Minimum packet size in bytes to trigger random drop
size_threshold=1000
# Maximum new connections per second before throttling
burst_rate=10
# Burst allowance for rate limiting
burst_packets=20
